# Runtime Systems — Junior

The runtime creates objects, manages calls, loads modules, reports exceptions, and may reclaim memory. Object headers carry type and lifecycle metadata. Stack frames preserve arguments, locals, return points, and exception state.

## Test yourself

1. What lives in a call frame?
2. Why does an object need type metadata?
3. What happens during exception unwinding?

Continue to [`middle.md`](middle.md).
