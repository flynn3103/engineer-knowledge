# Python HTTP APIs — Senior

Design APIs for evolution and operations.

- Publish compatibility and deprecation rules.
- Use versioning only at a meaningful contract boundary.
- Limit payload size, concurrency, and expensive queries.
- Propagate trace context and expose reliable health signals.
- Test timeouts, partial dependency failure, and backwards compatibility.

The framework is replaceable; the contract and operational behavior are not.
