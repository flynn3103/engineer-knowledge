# Modules and Dependencies

> Keep the build list explainable, reproducible, and safe to upgrade.

Work through module creation, cleanup, vendoring, version selection, proxy verification, graph pruning, and supply-chain controls in that order. Each topic includes four levels from running one command correctly to governing dependency changes across teams.

Start with [go mod init](01-go-mod-init/README.md), then use the navigation to advance through the module lifecycle.
